# mechine-learning-project

This project uses the **UCI Energy Efficiency dataset** to build both classification and regression models predicting building energy performance.

In code it have used chatgpt to help on the structure and error fixing 
## Setup
```bash
pip install -r requirements.txt
